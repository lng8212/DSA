#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >>n;
    int a[100007];
    for (int i=0;i<n;i++){
        cin >>a[i];
    }
    int k=a[0];
    if a[0]=0;
    for (int i=1;i<n-1;i++){
        if (a[i]=1 && a[i+1])
    }
    return 0;
}