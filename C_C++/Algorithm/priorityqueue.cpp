#include<bits/stdc++.h>
using namespace std;

int main(){
    //priority_queue<//kieu du lieu // vector<kieu du lieu>  compare>
    priority_queue<int> q;
    q.push(2);
    q.pop();
    q.top();
    q.empty();
    q.size();
    // mang thanh dau tien la q.top() luon lon nhat.
    
    return 0;
}