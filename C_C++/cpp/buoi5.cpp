#include<iostream>
#include<string>
#include<vector>
using namespace std;
struct sinhVien{
    string ten;
    int MSV;
    int diem;
};
int main (){
    //vector
    int n;
    cin >>n;
    vector<int> ds(n,0);
    ds.resize(n);
    ds.size();
    ds.push_back(), ds.pop_back();
    //string
    //vector<char> ... == string;
    string s;
    s=s+'b'+s+"dasd";
    if(s[i]>='A' && s[i]<='Z');
    if(s[i]>='a' && s[i]<='z');
    if (s[i]>='0' && s[i<='9');
    // con tro
    //vao ra file
return 0;
}